Ansible requires:
  conf/provision_ssh_key

This file should be a copy of your public key.

You may need to:
  - change the IP address in inventory
  - change that IP address in the flask_app.yml playbook

Running playbook:
  'ansible-playbook flask_app.yml'

You may get prompted for to accept the key into your known hosts on the first run.
